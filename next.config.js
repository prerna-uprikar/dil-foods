/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['dilfoods.in', 'luna-meraki.svc.urbanpiper.com', 'cdn.urbanpiper.com', 'your-image-url.com'],
      },    
}

module.exports = nextConfig
